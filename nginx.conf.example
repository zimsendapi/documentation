# Configuration Nginx pour la documentation ZimSend
# Copiez ce fichier vers /etc/nginx/sites-available/docs.zimsend.com

server {
    listen 80;
    listen [::]:80;
    server_name docs.zimsend.com;

    # Redirection automatique vers HTTPS (après configuration SSL)
    # return 301 https://$server_name$request_uri;

    root /var/www/docs.zimsend.com;
    index index.html;

    # Logs
    access_log /var/log/nginx/docs.zimsend.com.access.log;
    error_log /var/log/nginx/docs.zimsend.com.error.log;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;

    # Cache des assets statiques
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|otf)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Cache des fichiers HTML
    location ~* \.html$ {
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
    }

    # Main location - Support pour les routes Docusaurus
    location / {
        try_files $uri $uri/ $uri.html /index.html;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # Bloquer l'accès aux fichiers cachés
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
}

# Configuration HTTPS (après installation de Let's Encrypt)
# Décommentez après avoir exécuté: sudo certbot --nginx -d docs.zimsend.com

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name docs.zimsend.com;
#
#     ssl_certificate /etc/letsencrypt/live/docs.zimsend.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/docs.zimsend.com/privkey.pem;
#     ssl_trusted_certificate /etc/letsencrypt/live/docs.zimsend.com/chain.pem;
#
#     # SSL configuration moderne
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     ssl_stapling on;
#     ssl_stapling_verify on;
#
#     root /var/www/docs.zimsend.com;
#     index index.html;
#
#     # Logs
#     access_log /var/log/nginx/docs.zimsend.com.access.log;
#     error_log /var/log/nginx/docs.zimsend.com.error.log;
#
#     # Gzip compression
#     gzip on;
#     gzip_vary on;
#     gzip_min_length 1024;
#     gzip_proxied any;
#     gzip_comp_level 6;
#     gzip_types
#         text/plain
#         text/css
#         text/xml
#         text/javascript
#         application/json
#         application/javascript
#         application/xml+rss
#         application/atom+xml
#         image/svg+xml;
#
#     # Cache des assets statiques
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|otf)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#         access_log off;
#     }
#
#     # Cache des fichiers HTML
#     location ~* \.html$ {
#         expires 1h;
#         add_header Cache-Control "public, must-revalidate";
#     }
#
#     # Main location
#     location / {
#         try_files $uri $uri/ $uri.html /index.html;
#     }
#
#     # Security headers
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#
#     # Bloquer l'accès aux fichiers cachés
#     location ~ /\. {
#         deny all;
#         access_log off;
#         log_not_found off;
#     }
# }
